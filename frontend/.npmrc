# The project is installed from the public npm registry. Without this file npm
# falls back to whatever registry the machine is globally configured with, which
# on a corporate workstation is an internal mirror unreachable from outside the
# network — and `npm install` then fails on a clean checkout (NFR-13).
registry=https://registry.npmjs.org/

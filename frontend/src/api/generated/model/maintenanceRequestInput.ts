/**
 * Generated by orval. Do not edit manually.
 */
import type { MaintenanceCategory } from './maintenanceCategory';
import type { MaintenanceLocation } from './maintenanceLocation';
import type { MaintenanceUrgency } from './maintenanceUrgency';

/**
 * FR-36's submission. Multipart, because of the photographs.
 *
 * There is no `room_id` and that is the design: an «own room» request is
 * bound to the room of the submitter's active residency record, read from
 * the housing register. A client that could name the room could name
 * somebody else's.
 */
export interface MaintenanceRequestInput {
  /** @minimum 1 */
  building_id: number;
  category: MaintenanceCategory;
  location: MaintenanceLocation;
  /**
     * Required when `location` is `common_area` and ignored otherwise — a note beside a room resolved from the register would be a second, unreliable answer to «where».
     * @maxLength 255
     */
  location_note?: string;
  /** @maxLength 255 */
  title?: string;
  /**
     * @minLength 10
     * @maxLength 2000
     */
  description: string;
  urgency?: MaintenanceUrgency;
  /**
     * Up to three. The ceiling is configuration, is refused here as a 422 naming `photos`, and is refused again by a CHECK constraint.
     * @maxItems 3
     */
  photos?: Blob[];
}

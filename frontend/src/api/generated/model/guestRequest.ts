/**
 * Generated by orval. Do not edit manually.
 */
import type { GuestRequestInvitingResident } from './guestRequestInvitingResident';
import type { GuestRequestStatus } from './guestRequestStatus';
import type { GuestVisit } from './guestVisit';

export interface GuestRequest {
  id: number;
  building_id: number;
  building_name?: string;
  student_id: number;
  student_name?: string;
  /** The resident who invited, as the client draws them: a link to their card and the room beside it. Present when the request was loaded with its student, which every route that returns one does. */
  inviting_resident?: GuestRequestInvitingResident;
  guest_full_name: string;
  visit_date?: string;
  planned_from?: string;
  planned_to?: string;
  /** The departure deadline: the earlier of the end of the interval and the hour the dormitory stops admitting guests (`visiting_to`). */
  due_at?: string;
  status: GuestRequestStatus;
  status_label?: string;
  /** Null until approval. Issued only on the decision to approve, so that before it there is nothing to present at the post (§3.5.1). Eight characters from an alphabet with the confusable ones removed, because it is read aloud and typed in at a desk. */
  access_code?: string | null;
  /** Null on a request the scheduler closed. FR-17's fourth criterion treats an undecided request as rejected, and nobody decided it — so the row does not claim an officer did. */
  decided_by?: number | null;
  /** The officer who approved or refused, by name. Null on a request the scheduler closed, for the same reason `decided_by` is. */
  decided_by_name?: string | null;
  decided_at?: string | null;
  decision_comment?: string | null;
  visit?: GuestVisit | null;
  created_at?: string;
}

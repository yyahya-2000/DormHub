/**
 * Generated by orval. Do not edit manually.
 */
import type { LostFoundCustody } from './lostFoundCustody';
import type { LostFoundItemKind } from './lostFoundItemKind';

/**
 * FR-24's publication form, `multipart/form-data`.
 *
 * There is no `status` field and no `reporter_id` field, and neither is an
 * omission: publication passes through no approval step, so there is no
 * state to ask for, and the record is bound to the publishing account, so
 * there is nobody to name.
 */
export interface LostFoundItemInput {
  building_id: number;
  /**
     * FR-24's «category», in the shape a dormitory uses it — the short line naming the object.
     * @minLength 3
     * @maxLength 255
     */
  title: string;
  /** @maxLength 2000 */
  description?: string;
  /**
     * @minLength 3
     * @maxLength 255
     */
  place: string;
  /** The day of the finding. Not later than today; nothing refuses an old one, because somebody clearing a desk drawer in December may well be publishing what they picked up in September. */
  happened_on: string;
  /** The day the find was declared to the police or to a local self-government body. Optional on both paths and required on neither: art. 227 cl. 2 puts the duty on the finder where the entitled person is unknown, and whether the university files such declarations is the university's to settle. Not before `happened_on`, and not in the future. */
  declared_on?: string;
  kind?: LostFoundItemKind;
  custody?: LostFoundCustody;
  /** FR-24, third criterion: optional, and at most one. Validated for type and size; the stored name is generated and never the client's. */
  photo?: Blob;
}

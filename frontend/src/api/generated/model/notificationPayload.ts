/**
 * Generated by orval. Do not edit manually.
 */

/**
 * The body of the message. Its shape depends on the category — each notification decides its own — so it is deliberately open here rather than enumerated: a category added in a later increment must not require this document to change for the existing ones to keep working.
 */
export type NotificationPayload = { [key: string]: unknown };

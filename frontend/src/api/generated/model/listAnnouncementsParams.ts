/**
 * Generated by orval. Do not edit manually.
 */

export type ListAnnouncementsParams = {
/**
 * Return only the announcements stored under this exact label. The category is a free string, so a label nobody has posted under is an empty page rather than a 422.
 * @maxLength 32
 */
category?: string;
/**
 * Return the expired announcements instead of the current ones. Sent as a query string, so `true`, `false`, `1` and `0` are all read — which is what a generated client produces from a boolean. Anything else is 422 naming the field.
 */
archived?: boolean;
/**
 * @minimum 1
 */
page?: number;
};
